# Final Code Drop (Web Upload)

This repo includes:
- Confetti feedback on successful task check-ins
- League feed + notifications pages
- Local-date (timezone-safe) fixes in league hooks
- Supabase migration for `user_notifications` + `league_events`

## Supabase (required)
Run the migration:
`supabase/migrations/20260216194500_notifications_and_feed.sql`

If you don't use the Supabase CLI, paste that file into Supabase SQL Editor and run it.

## GitHub Web Upload
See assistant instructions in chat: upload files to a new branch, open PR, merge when ready.
